  python3 <<'PY'
  import imaplib
  from pathlib import Path
  c = imaplib.IMAP4_SSL('london.mxroute.com', 993)
  c.login('companies@calvelli.us', Path('/run/agenix/email-companies').read_text().strip())
  for f in ('INBOX', 'Trash'):
      c.select(f)
      typ, d = c.search(None, 'ALL')
      print(f'{f}: {len(d[0].split())}')
  c.logout()
  PY
